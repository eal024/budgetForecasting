

list( normalityText = "")

"The mean of the residuals should be close to zero. A normal distribution around zero, indicates this.    
The Shapiro-Wilk test is a formal hypothesis test for normality. I The null hypothesis is that the errors are normal, a small p-value would cause us to doubt the normality assumption.  
Execting ???shapiro.test(residuals)???, gives a P-value equal 0,17.  

